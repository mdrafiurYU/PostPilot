{
  "name": "@postpilot/repurposing-engine",
  "version": "0.0.1",
  "description": "AI-driven clip extraction, transcription, and caption generation",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "pg": "^8.12.0",
    "@postpilot/events": "workspace:*",
    "@postpilot/queue": "workspace:*",
    "@postpilot/types": "workspace:*",
    "@postpilot/logger": "workspace:*"
  },
  "devDependencies": {
    "@types/pg": "^8.11.6",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
