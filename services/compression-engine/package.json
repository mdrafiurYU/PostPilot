{
  "name": "@postpilot/compression-engine",
  "version": "0.0.1",
  "description": "FFmpeg-based video/image compression with VMAF scoring",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "@postpilot/events": "workspace:*",
    "@postpilot/queue": "workspace:*",
    "@postpilot/types": "workspace:*",
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "@postpilot/logger": "workspace:*"
  },
  "devDependencies": {
    "@types/pg": "^8.11.6",
    "fast-check": "^3.19.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
