{
  "name": "@postpilot/api-gateway",
  "version": "0.0.1",
  "description": "API Gateway / BFF layer with routing, auth middleware, and WebSocket proxy",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "@postpilot/events": "workspace:*",
    "@postpilot/types": "workspace:*",
    "express": "^4.19.2",
    "express-rate-limit": "^7.3.1",
    "http-proxy-middleware": "^3.0.0",
    "jsonwebtoken": "^9.0.2",
    "@postpilot/logger": "workspace:*"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.6",
    "@types/node": "^20.0.0",
    "@types/supertest": "^6.0.2",
    "supertest": "^7.0.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
